export {};

declare global {
    namespace ReactNativePaper {
        interface ThemeFonts {
            bold: ThemeFont;
        }

        interface ThemeColors {
            success: string;
            warning: string;
        }
    }
}
