//#region Imports

import { StyleSheet } from 'react-native';

//#endregion

const useTextSnackbarStyles = () =>
    StyleSheet.create({
        text: {
            color: '#FFF',
            flexWrap: 'wrap'
        }
    });

export default useTextSnackbarStyles;
