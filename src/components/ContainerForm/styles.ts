//#region Imports

import { StyleSheet } from 'react-native';

//#endregion

const useContainerFormStyles = () =>
    StyleSheet.create({
        container: {
            flex: 1
        }
    });

export default useContainerFormStyles;
