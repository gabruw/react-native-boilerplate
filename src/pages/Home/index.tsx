import { ReactElement } from "react";
import { Text, View } from "react-native";

const Home = (): ReactElement => {
    return (
        <View>
            <Text>It's Home</Text>
        </View>
    );
};

export { Home };
