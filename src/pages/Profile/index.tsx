import { ReactElement } from "react";
import { Text, View } from "react-native";

const Profile = (): ReactElement => {
    return (
        <View>
            <Text>It's Profile</Text>
        </View>
    );
};

export { Profile };
