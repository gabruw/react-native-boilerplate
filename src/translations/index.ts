import { Resource } from "i18next";

import { enUS } from "@app/translations/enUS";
import { ptBR } from "@app/translations/ptBR";

const translations: Resource = {
    ptBR,
    enUS,
};

export { translations };
